import 'package:flutter/material.dart';

class NetworkingTransformer extends StatelessWidget {
  const NetworkingTransformer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Prueba de networking"),
    );
  }
}